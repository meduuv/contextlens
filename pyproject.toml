[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "contextlens"
version = "0.1.0"
description = "Context size statistics"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/contextlens"]
